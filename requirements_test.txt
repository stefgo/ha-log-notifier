# Die Tests kommen ohne laufendes Home Assistant aus: Geprüft wird die reine
# Logik (Levels, Store, Ingest-Parsing, Rate-Limit) plus die Konsistenz von
# Manifest und Übersetzungen.
pytest>=8.0
ruff>=0.8
mypy>=1.13
