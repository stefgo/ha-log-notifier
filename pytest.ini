[pytest]
testpaths = tests
# The test modules put the component directory on the path themselves; without
# importmode=importlib, modules with the same name collide during collection.
addopts = -q --import-mode=importlib
# conftest.py registers the component directory; with the importlib import mode
# the test directory itself is not automatically on the path.
pythonpath = tests
