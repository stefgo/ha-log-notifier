node_modules/
# Build target of the card — the integration serves it from there.
custom_components/lognotifier/www/
__pycache__/
*.pyc
.pytest_cache/
.venv/
.DS_Store

# Contains the target host of a private instance — builddeploy.sh itself is
# tracked and reads it.
.env
